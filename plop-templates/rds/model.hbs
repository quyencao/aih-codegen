'use strict'
module.exports = (sequelize, DataTypes) => {
  const {{{ ModelName }}} = sequelize.define('{{{ TableName }}}', {{{ Fields }}}, {timestamps: false, tableName: '{{{ TableName }}}'})
  {{{ ModelName }}}.associate = function(models) {
    // associations can be defined here
  }
  return {{{ ModelName }}}
}